namespace SpaceShip.Domain.Model.State
{
    /// <summary>
    /// State for paceship models.
    /// </summary>
    public enum SpaceshipState
    {
        Start,
        Sleep,
        Finish
    }
}
