namespace SpaceShip.Domain.Model.State
{
    /// <summary>
    /// State for Resource models.
    /// </summary>
    public enum ResourceState
    {
        Start,
        Sleep,
        Finish
    }
}
